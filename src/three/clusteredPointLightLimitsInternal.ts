/**
 * Three-free limits shared by the Studio document validator and clustered
 * adapter. Keeping these numbers here lets plain-data validation stay free of
 * renderer objects and WebGL allocation side effects.
 */
export const MAX_UNBOUNDED_CLUSTERED_POINT_LIGHTS_INTERNAL = 8;
