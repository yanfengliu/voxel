# Devlog Summary

- 2026-08-14: **A live scene stops pretending to have a timeline, and the windmill relocation proof stops passing on sail phase** — a scrubber offers a position in a recording, and Machine Works, the chain, the ball drop and the playground stations have none; the bar hid its scrubber and speed control for those and reads **"live · N solver steps"** instead. Play/Pause there was worse than useless: it read the scene clock, parked at zero on a scene with no authored motion, so a running machine was labelled "Play" **and the button that would have stopped it was disabled**. It drives the solver now. The windmill's eight composed-scene relocation proofs turned out to compare a **running** mill against a **static** variant — the tell was four different variants returning the same rear-view number to four decimals — so a large part of every measured "relocation" was the sails having moved. Both sides are held at tick zero now, which made the comparison byte-identical across runs, and each relocation is judged from two cameras that **frame the move**, worth 2-9x the detection of a whole-mill view. The floor falls from 0.012 to **0.00074**, half the smallest real relocation (the anvil's one-grain move, 0.0014812), because the 0.024441 the old floor was derived from was the artifact. Confirmed to bite by zeroing a relocation delta.
- 2026-08-14: **Riverfall reads as a waterfall, after the flat sheet was measured and found to have one visible channel** — the owner's "too peaceful" was right and the cause was not the solver: every tile is one flat colour, so only the outline of the water ever showed it moving, and in the studio's default unlit look the whole scene changed **0.75-1.6% of stage pixels per 200 ms**. Quadrupling the displacement and tripling the tilt gain bought 1.8-2.7% and the curtain stayed a slab. Three things together took it to **32-35%** framed on the fall and **4.8-5.1%** at the default wide camera: 96 foam flecks riding the solver's own particles, three point lights the reconstruction's tilt channel had been waiting for since it was written, and a phase speed from 5 to 12 with a tilt gain from 8 to 16. The wavelength stayed at 20 because it **cannot** go lower — tiles sit two units apart and the p95 adjacent-height delta measured 0.1178 at wavelength 8, 0.0989 at 13, and 0.0874 at 16 against a 0.08 checkerboard gate. This overturns the scene's pinned "does not overlay a separate drop or foam-particle layer", with the owner's decision and the measurement behind it recorded where the test used to be. Both fleck pools are sized from a measured supply floor (79 particles in the curtain band, 511 in the drawn reach, at their emptiest across a minute) so no fleck is ever left with nowhere to be; a test re-runs that minute. Two earlier tunings are recorded as wrong in the source: a 0.7-unit cube fleck rendered as a floating crate, and a 1.4-unit clearance visibly hovered where 0.9 is what the geometry actually needs.
- 2026-08-14: **One pointer rule and one simulation switch, after both turned out to be several** — the left button fell back to an orbit whenever it caught nothing, so reaching for a swinging chain link was the likeliest way to fling the camera; left now consumes its own drag and never turns, middle turns, right pans, in the model stage, an editable scene, a self-posed scene, and Interact alike. The simulation switch knew only the scene clock — animated recipes, moving lights, pose replays — which is why **Machine works showed no switch at all** (no animated recipe, no light, every moving part the solver's answer) while **Riverfall showed one that left its water running**. Both halves are one switch now, hidden only when the scene has no motion of any kind; stopping it holds the solver with no step, no pose rebuild, and no redraw. Two browser tests pin it, both confirmed to fail on neutralized code. Unifying the switch also exposed a gate passing on noise: the windmill composed-scene review proofs compare a **running** mill against a **static** relocation variant, and with both sides genuinely held still four of the eight relocations move 0.000171 to 0.011677 of the frame from the front and **exactly zero** from the rear, against a 0.012 floor that was itself calibrated from running-mill numbers. Left exactly as it was, with the numbers recorded, because redefining that gate is not this change's job. Measured on the way past, for the Riverfall complaint that opened the session: the water's only visible channel is its silhouette — **0.75–1.6% of stage pixels change between frames 200 ms apart**, and cranking the normal excursion 4× and the tilt gain 3× moved that to 1.8–2.7% while the curtain stayed a flat slab, because every tile is one flat colour and the fall's tile normals face the camera.
- 2026-08-13: **The last four review findings closed, and two of them changed shape once measured** — the "minutes of duplicated solver work" was **0.7 s**: one 32-second Machine Works solve is 623 ms, so sharing the run would trade test independence for nothing. Its real half was the bare `900_000` and `600_000` budgets sitting above the floor where the meta-scan cannot see them, now derived from measurement. The windmill floors were worse than reported and wrong in a more interesting way: 50 foreground pixels against a real minimum of **5,734** across 138 samples, and 0.01% changed pixels against a tightest real relocation of **2.44%** — but raising the shared floor broke four removal proofs, because removing one exact box legitimately changes **0.000359** of the frame. One constant was serving two populations two orders of magnitude apart; they are separate now, each at half its own measured minimum. Capture gained an optional `outputColorSpace` and refuses a renderer that does not write sRGB, which the spec had promised and nothing checked. `ChunkIndexV1`'s slot map stays unbounded **by necessity**: a coordinate with no slot restarts at generation 1, so dropping a tombstone reopens the ABA hole `chunk-index.test.ts` already pins — bounding it needs an epoch-replacement path the plan caller does not have. Mechanism recorded at the site.
- 2026-08-13: **Five more fixed after the deferral list was challenged, and one of them rejected valid input** — the chunk-overlap sweep sorted on `x` and broke out when a chunk began past the current one's end, so a world *thin in x* never hit that break: **1,400 chunks in one slab accepted, 1,415 rejected** with zero overlaps. It sorts on the axis the chunks actually spread along now, the duplicate copy in `delta-final-graph.ts` is gone, and profiled worlds skip the sweep entirely because a uniform grid already proves the partition. A playground scenario's `status` folded `performance.now()` into a verdict whose own doc comment said timing was "never part of the verdict inputs" — that had already cost a helper and three comments working around 62 ms and 81.48 ms steps flipping green runs red on a loaded host. A `contactPolicy` resolved zero colliders for a spawn-only body and silently granted it Rapier's defaults; the combination is refused now. `#jointedBodies` was add-only, so bearing friction at 25-40x air-spin outlived the joint. Eight browser specs asserted nothing about page errors, including the Interact lane — a hook now covers all 34 tests, verified by injecting a `console.error` and watching five go red. Four findings stay open with reasons: the bound, the measurement, or the contract change each needs is the actual work.
- 2026-08-13: **A full-codebase review fixed eight defects and reverted a ninth, and three of the tests written for them passed on unfixed code first** — `Array.from` in both validators consulted the caller's `Symbol.iterator` after bounding `length`, so a probe got **10,000 yields out of an array declaring `length: 0`** and an endless iterator hangs `acceptSnapshot` on the caller's thread. A worker `protocol-error` carries no job id by design, so the scheduler read the active job's only reply as somebody else's stale output, discarded the diagnostic, and left the slot busy forever — there is no timeout anywhere in `src/`. `animate` cleared every queued update range before adding its own, and Three uploads exactly the ranges present at draw time, so a delta moving a non-animated instance never reached the GPU while the metrics counted the write. `resize`/`setView` never retired the presented manifest, so a capture after a resize returned new-size pixels under the old viewport and camera. The playground's two lanes are not one world — the headless twin gives every dynamic body soft CCD and the live lane gives it to none, **0.16427 m of burial against 0.00342 m** — but declaring it took the live trebuchet from **23 bricks knocked to zero** while the twin's 19 scenarios stayed green, because none of them assert the wall coming down; reverted and recorded, since matching the twin assumes the twin is right. `deleteStudioScene` was a drifted copy of `closeSceneMode` missing exactly the two calls that stop a live solver. `fixtures/chain-consumer` was in no TypeScript program and yielded three real errors on first check. A twenty-line comment told every reader the playground had not reached 60 Hz, directly above the line deriving it from the shared rate. HEAD packed `voxel/physics` as `1.0.0`; it is 1.1.0 now. Eight findings recorded and left, with reasons.
- 2026-08-07: **`## Known traps` is gone from AGENTS.md, and its eight entries went four different ways** — FLEET.md now refuses the section outright ("a trap the repo paid for is a lesson... a contract the system must satisfy goes to Invariants"). Three became lessons with new evidence entries anchored to the commits that paid for them: `73f9bbc` (the 60 Hz move exposed a carrier tip that was never implemented, and a run test building its own world), `eb1260e` (953 green tests while the flour climbed out through the roof, then the far sail reading as a sack on the roof), and `08ca50e` (two tests written to a reviewer's claim passed with and without their fix). Contact declaration (`be30dd8`, `contactPolicy`) states what must be true rather than what was learned, so it moved to Invariants — no design doc held it, and `contactPolicy` appeared nowhere outside the trap. The shared-worktree gate entry folded its one non-duplicated half — the throwaway-worktree remedy — into local-rules' Concurrency section, which already carried the prevention rule and its measured cost. Two were deleted: the rate-gate entry restated [`A rate gate can reject a working machine`](../learning/lessons-evidence.md#a-rate-gate-can-reject-a-working-machine-without-becoming-an-output-proxy) almost verbatim, numbers and candidate key and all; the three-renderer-false-positives entry dates to the repo's first commit `7fbae42` with no incident behind it ever, and its three halves are already held by the Gates visual-inspection paragraph, the min-of-batches lesson, and the canon's own `c024b33` citation. Lessons index 48 → 52 lines against its 120 ceiling; AGENTS.md 82 → 72. Doc only.
- 2026-08-07: **The lessons file became readable at session start, and two local rules turned out to be stale rather than strict** — all 21 entries moved verbatim to `docs/learning/lessons-evidence.md` and `lessons.md` became a grouped index of one-line claims, **279 lines → 46**. `tests/testing/lessons-pairing.test.ts` pins both directions, the non-vacuity of its own parsers, and the index length; a deliberately broken anchor was confirmed to fail it. `local-rules.md` was quoting a canon line that no longer exists (`Concurrent sessions share one worktree and one index` was reworded to `You are not the only writer in the worktree` on 2026-08-07), and its evidence-anchor rule — kept locally after the 2026-08-04 sync trimmed it from FLEET.md — is fleet canon again, so the local copy went. AGENTS.md's "add heavier devlog/decision/progress docs only when recurring work makes them useful" was deleted outright: it deferred a devlog on a condition nothing ever evaluated, which the canon now forbids. This file is the result. Doc and test only; no engine or renderer change.
- 2026-08-07: **The suite reported 15 failures across 11 files on a markdown-only diff, then 1891/1891 green minutes later** — recorded rather than reran-past, because [`A timeout sized against the suite's current load is a time bomb`](../learning/lessons-evidence.md#a-timeout-sized-against-the-suites-current-load-is-a-time-bomb) says a failure on a diff that cannot have caused it is the defect reporting itself. The diff was `docs/**` plus one new test that reads two markdown files; nothing in the failing set imports any of it. The failing run showed transform 350s and import 474s against a 220s wall clock, so it was heavily contended; the clean run was not. Only one failing file was identified before the log was overwritten — `tools/studio/catalog-diversity.test.ts`, around `analyzeStudioCatalogDiversityV1(createStudioCatalog())`. **Resolved the same day — see the entry below.** It was a timeout, and the prescription written here was not sufficient.
- 2026-08-07: **All 15 failures were `Test timed out in 5000ms`, and the fix the entry above prescribed would not have worked** — reproduced exactly (11 files, 15 tests, 1,876 passing) under 24 competing CPU workers on 32 cores, with the log captured first this time. The tests do 570–1,694 ms of work alone and 5,425–11,708 ms under that load; none had chosen a budget at all, inheriting vitest's unstated 5,000 ms default. **Four times the measured work — the `RIVERFALL_HEAVY_CASE_TIMEOUT_MS` rule — is 2.3 s for the shortest, below the default it had already blown**, because contention costs a roughly fixed scheduling delay on top of the multiplier and short tests therefore stretch furthest (570 ms → 10.8×, 9,629 ms → 5.5×). `tests/testing/test-timeout.ts` makes a budget `45,000 ms + 4 × measured`; the config's global default is that rule at zero work, and `RIVERFALL_HEAVY_CASE_TIMEOUT_MS` now derives from it (180,000 → 222,600). Ten hand-written literals at `15_000`/`30_000` were **below** the default and so opted their tests out of it — worse than writing none — including two in `riverfall-fluid-simulation.test.ts` itself, which duly failed at 41.8 s and 52.7 s on the first run after the partial fix. **My first inventory of existing timeouts ended in `head -40` and never reached `fixtures/`, which is exactly why that partial fix shipped.** A gate in `test-timeout.test.ts` now scans every test file and fails on any budget below the allowance; confirmed to bite. Gates: typecheck clean, lint clean, and the full suite green under the same 24-worker load at 226 files / 1,898 tests.
