# 2026-09-05

## Camera navigation reuses the current subject

The continuation started from `d81851f` with only the owner's unrelated `AGENTS.md` edit present. That checkpoint was pushed to `origin/main` after explicit authorization. Ordinary `npm run oak` inspection at 1280 by 720 and DPR 1 in headless Chromium 147.0.7727.15 used ANGLE SwiftShader on an i9-13900KF; the HUD sampled 11.6 FPS during early growth and 6.8 FPS with wind after the rain cue had ended. Those software-rendering samples establish neither hardware performance nor a sustained rate. The initial probe used a nonexistent biological-time property and recorded null days; its host ticks and FPS remain valid, and the subsequent fixed-state benchmark reads `elapsedBiologicalSeconds` and confirms day 100.

Independent source investigation found that pointer, wheel and held-key navigation rebuilt every transformed subject vertex even when retaining the free camera. Preparing the subject bounds also allocated two temporary `Vector3` objects per vertex. A private host-owned fitter now retains one prepared subject by immutable render-snapshot identity and focus, reprojects it for each camera change, invalidates on frame replacement or focus change, and clears on disposal. The existing stateless fitter still reads mutable caller data on every call. Refitting-only depth work now runs only when a view is actually refitted.

The fixed day-100 frame has 120,984 subject vertices. After ten warmup pairs, sixty measured pairs of alternating cached/uncached retained-camera fits in the same browser measured cached median/p95/max of 1.7/2.3/4.3 ms and uncached 7.2/12.8/25.9 ms. Every paired fit result matched exactly. This measures CPU camera fitting against the stateless path on unchanged geometry, not RAF cadence, fresh growth frames or GPU completion. The measurement probe imported a separate Three module instance and emitted its duplicate-instance warning; both timed arms used that same camera instance. Ordinary host inspection recorded console errors only and cannot establish the absence of warnings. Local raw measurements and capture digests remain under ignored `output/playwright/oak-2026-09-05-continuation/`.

The reviewer caught that the first cache test merely selected cutaway focus on an ordinary frame whose root batch was empty. The corrected regression builds real cutaway frames, requires nonzero roots at days 100 and 249, and runs ordinary to cutaway to ordinary on one fitter. It also covers same-revision geometry replacement and reset, explicit clear, and stateless same-object matrix mutation. Forcing every cache lookup to rebuild produced the expected failure: one preparation was required but four occurred. The production condition was restored before verification.

Mature hero, side, overhead and root-cutaway captures were inspected individually at 1280 by 720. Framing remains useful; thin rail-like side silhouettes, broad soil terraces and acorn/cutaway grid artifacts remain visible. Exact leaf material contact, visual acceptance and sustained 60 FPS remain unresolved.

Independent code review and focused test re-review completed before final verification. The camera source stayed unchanged while the separate policy-only commit `ba71a98` moved the base. The final local `npm run verify` exited successfully: 292 unit-test files and 2,282 tests passed, with one file and one test skipped; all 133 browser tests passed in 14.5 minutes, with worst timeout-budget use of 46%. Build, typecheck, lint, compatibility, public API, package and supply-chain checks also passed. The retained log is `output/playwright/oak-2026-09-05-continuation/verify.log`.

## Pixel measurements must follow the scale being inspected

The first-flush browser checkpoint demanded visible leaf-colour pixels at day 20, but the accepted frame contains only three petiole segments with transverse dimensions of roughly 20–70 micrometres. The 960 by 720 hero projection measures a maximum transverse diagonal of about 0.536 pixels. The checkpoint remains as first-petiole evidence and checks its exact population and subpixel bound. A separate day-24 checkpoint requires more than 100 leaf records and visible lamina colour. This is a correction to the instrument, not an enlargement of biology to satisfy the screenshot.

The closer root camera exposed the opposite error. The root-contrast instrument sampled supposed soil nine pixels either side of each root station. Those coordinates lay inside the enlarged coarse root, yielding three contrasted stations and a zero median width despite the visible shaft. The instrument now compares each pixel with a same-camera plant-hidden frame. A synthetic three-pixel and 32-pixel shaft test catches both scales, and an absent-root counter-frame must yield zero contrast and width. The original four-station, two-pixel-width and luminance-contrast floors are unchanged.

## A support envelope is not occupied material

An independent attachment review reproduced a fixed node datum lying 0.223767138773 millimetres outside a rolled petiole section even while both supporting-plane equalities passed. The code and tests now call the model a kinematic datum with conservative clearance. The ordinary controller is checked immediately before and after fracture, and the litter-offset test separately accounts for vertical terrain support. No physical-contact repair is claimed.

## Native-size review did not establish visual acceptance

All 35 retained oak PNG baselines were inspected individually at 960 by 720, including early growth, mature hero/side/overhead, root cutaway, senescence, falling litter and weather states. Their sorted `path<TAB>sha256<LF>` manifest has SHA-256 `d3d077a4b774ca3fc6cf0b03cf260be9e31f90a240d51e01de53b60c3180562b`; this review does not transfer to regenerated bytes. An independent critic inspected the mature overhead and side, first-petiole hero and litter overhead against matching digests.

Voxel construction and distinct lifecycle states are visible, but the side silhouette still has rail-like blades, soil still forms broad terraces, and fine dotted or grid artifacts remain around the acorn and cutaway. Litter spacing looks arranged and its centre ribs remain conspicuously green. The review therefore preserves the baselines as regression inputs, not beauty acceptance. Physical material contact, resolved root biology, adult-tree realism and sustained 60 Hz remain delivery gaps. FPS text is masked only in the fixed HUD images; the browser HUD tests check it unmasked.

## The expanded sweep outgrew its old timeout

The first isolated full verification passed 2,276 unit tests and all 133 browser tests, but failed the browser headroom gate: the fifteen-milestone sweep took 156.6 seconds of its former 180-second allowance. Running that test alone reproduced 160.5 seconds and another headroom failure. Its timeout now derives from `timeoutForMeasuredWorkMs(160_500)`; assertions, milestones and screenshot tolerances are unchanged. This is a test scheduling budget, not a renderer performance claim.

The full run also left its idle headless browser parent alive after every child exited. The verified task-owned parent needed explicit process cleanup before the runner returned the failing headroom verdict. The focused follow-up exited normally without intervention. No browser dependency or shutdown code was changed on this one observation.

The repeated full `npm run verify` on the policy-only `954bc10` base passed: 2,276 unit tests with one skipped, all 133 browser tests, and the package, compatibility, type, lint and supply-chain gates. The browser suite finished in 14.1 minutes, its reported worst headroom use was 43%, and its browser exited normally without intervention. This verifies the bounded checkpoint, not the unresolved realism and performance goals above.

## Space reaches the existing pause command

The owner's request started from `58caca247637438f950263cd4762eaddacc476ca` with the owner's `AGENTS.md` edit preserved. The explicitly designated orchestrator delegated controls, presentation, geometry and physics work under separate file ownership. Oak already had a pause command, but its Studio keyboard adapter reported both the scene and motion unavailable and supplied an inert playback callback. The adapter now routes Space through that existing command once per press, subject to host readiness and the bounded command queue. Editable controls, native keyboard controls, modifiers, composition and repeat events retain their existing guards. Pausing stops automatic biology and weather advancement while leaving camera navigation available; resuming discards paused wall time. Explicit deterministic harness advancement remains available while paused.

The HUD moves biological age, pause/resume, status, frame rate and environmental readings into its fixed header above scrolling controls, including the active resource regime. The light panel, pale background, cooler sky fill, lower sun intensity and less saturated soil palette are presentation choices. Lighting disposal restores its owned background without overwriting a later host replacement. Leaf camber distributes its two transverse risers across each half-blade instead of stacking them beside the midrib; paid sample count, growth order, outer width and total rise are retained. This does not repair the material attachment or replace the falling trajectory.

The controls worker reported four passing browser tests, six passing unit tests, typecheck and lint. The two new cases in `tests/browser/oak-ecosystem-keyboard.spec.ts` exercise real keyboard input and deterministic animation timing, including rain, camera movement during pause and a twenty-second pause without catch-up. Reintroducing the disabled router made the pause assertion fail with expected `Resume` and actual `Pause`, then the working router was restored. The geometry worker separately reported 22 focused passing tests. Independent read-only review of the controls and lighting found no material issue. These are scoped worker results; the clean integrated result is recorded below.

The expanded `oak-ecosystem-hud.spec.ts` checks the pinned values, 44-pixel command targets and scrolled interactions at a 960 by 520 viewport. A preview impression that the title was clipped was rejected by native-byte measurement: the title region of captures 05 and 06 is identical, the heading remains at 23.390625 px and the panel at 12 px, and only the control body scrolls. No source clipping defect was established. The measured replay and manifests remain in ignored `output/playwright/oak-beauty-2026-09-05/workflow-evidence/panel-scroll-probe/`, with the capture review in `presentation-v3/review.md`. Two speculative CSS mutations had left the HUD test green and are not mutation proof; no lesson is retired on that basis. Historical captures and verification results above apply only to their own revisions and bytes.

## Senescence colour follows the existing tissue state

Native review caught green axial strips in otherwise brown detached leaves. The first colour change covered structural roles but missed lamina near the axis. The final branch quantizes actual chlorophyll for every senescent material role using the existing anatomical order, with edge cohorts losing pigment before axial cohorts. The living branch, source geometry, identity and resource pools are unchanged. `oak-leaf-material-phenology.test.ts` checks all ten default seasonal fractures, bounds the preceding one-tick colour change, compares attachment-only RGBA changes and requires exact donor-to-day-249 litter RGBA identity. Living and maturing controls also retain green tissue and prevent green restoration after pigment loss.

The first counter-run failed on green structural tissue; the expanded all-role counter-run then failed on a near-axis lamina cell with red 63 and green 141. The final focused run passed 12 tests in three files. The separate 15-test continuity run began before the final all-role source and does not validate that later candidate. Independent read-only review found no material issue in the final controls, camber, lighting and colour changes. The reviewed anatomy file has SHA-256 `5fd0b085d5b370f459e003874cc00e39c9aab65b9d4b4123b7aaa903feaf00e3`; ignored red and focused logs remain under `output/playwright/oak-beauty-2026-09-05/workflow-evidence/`.

The independent native review covered all 11 final material and weather PNGs at 1280 by 720, including healthy and late-season tissue, litter and three weather angles. Their sorted `name<TAB>sha256<LF>` digest is `094f790689b20acddc0ae8930d6a954ebf15a27759d3cc45b9b584fab7e6acd7`. Brown tissue now continues through detached leaves and litter, while blue-gray airflow remains distinct from brighter cyan rain. The capture manifest in `pigment-v2/` records unchanged start/end source diff `84918d08a5fc9d19b1641d578bfff10f325de2b5d9fb8587d732f908d735e757` on base `58caca2`. These are bounded material observations, not complete beauty or physics acceptance.

The same critic inspected all 29 decoded frames from ordinary growth, weather and fall clips individually in temporal order and verified their hashes plus the three video hashes. The decoded-frame set digest is `ac75759af0cb281ac7f46e965fd54be3b02796c04d91f02b15491c540dd8b006`; `final-motion/decoded-frame-review.json` records the files. This is ordered sampled-frame review, not uninterrupted playback. Wall-clock `recordingElapsedSeconds` does not align with encoded presentation time. Corrected decoded pairs hold day 28.42 during growth, wind 4.00 m/s during weather and day 247.57 during fall, then visibly resume. A suspected paused blade change had a maximum channel difference of 17/255 across the scene and only 491 pixels above 8/255; this did not establish geometric motion. The retained comparison supports stable observed poses and state, not byte-identical lossy video.

Camera code remains unchanged. Fitting the complete plot made early plants too small in the measured trial, so the broad cropped terrain remains a composition limit. Board-like early blades, basal grid marks, reduced root depiction, separated attachments and arranged litter remain visible.

The first baseline refresh passed 14 browser cases but used the default `changed` mode, which retained old seed bytes inside screenshot tolerance. Explicit `--update-snapshots=all` then replaced the complete 35-image scope, whose final set digest is `423f63eb44b96704a3956b76837eb88c8982f5b9e5c5fa9d4b399a3bb3c4c2aa`. The visual worker inspected all 35 native images; the independent critic inspected 21 covering early growth, three mature angles, roots, every senescence and fall phase, litter, weather and the full HUD. That subset has digest `0c7c69b85f2fd4c8424e70727a1a9adaa17d2b6e6182d166cc5f21408b25164d`. All 35 file hashes match `baselines/after-manifest.json`, and the product-only patch plus four untracked product files were identical to the motion capture at that boundary. The recapture's four cases passed, but the command exited 1 because framing spent 173.1 seconds of its 180-second budget, or 96%. An isolated ordinary comparison then passed at 99.8 seconds, or 55%, without regenerating images. The slower run's cause was not established, and its headroom failure is retained.

## A successful command still contained a flaky keyboard gate

The first final `npm run verify` exited 0 with 2,288 unit tests passing, one intentional opt-in test skipped, 134 browser tests passing and one flaky keyboard test that passed its retry. The orchestrator rejected that as clean acceptance. The trace stops in the keyboard test's unacknowledged event-observer Promise after real Space input completed; the captured page already shows paused biology and falling rain. Missing registration ordering is consistent with the trace, but the trace does not directly record listener installation. Cleanup then tried to evaluate an already closed page, obscuring the wait. The original log and archived trace remain under ignored `output/oak-final-verification/`.

The test now awaits observer installation before sending Space and checks the nullable observation with a bounded poll. Shared cleanup restores RAF in `finally` and suppresses only the observed closed-target error when the page is already closed. Other cleanup errors still fail. The optional injected harness uses a partial type because the first test has no RAF override; lint rejected the earlier complete type. Independent review of final test SHA-256 `c08b3604869aff8e57361204a8f79e67834ef74f57f0ca71c17d25a0e0ff06b8` found no material issue. The clock, rain, camera, focus and native-control assertions and timeout are unchanged. The final two tests passed three repetitions each with retries disabled in 27.0 seconds; the slowest took 6.5 seconds of its 110.5-second budget. Typecheck and scoped lint also exited 0. This test-only repair left product runtime and baseline bytes unchanged.

## The reviewed checkpoint passed the final local gates

The repeated `npm run verify` on Node 24.12.0 and base `58caca2` exited 0 in 20 minutes 54.793 seconds. It passed 294 unit files and 2,288 tests, with the explicit opt-in `windmill-compact-physics-search.worker.test.ts` remaining skipped. All 135 browser tests passed in 15.4 minutes with no retry, flaky result, failure or browser skip. The slowest budget use was framing at 87.1 of 180 seconds, or 48%. Build, typecheck, lint, compatibility, public API, package and supply-chain gates also passed; both dependency audits reported zero findings.

The source digest `1403256fc5cf2da15723b4b0fca4d814b9a054ee787fd3a4c91ff6dbdbb75db4` and the complete 71-baseline digest `4e24b46f89872a7b2a963379f05b444ea062f3c64d5ee8e17a898fa236e1e871` remained unchanged across that run. The exact log, result, manifests and cleanup proof remain in ignored `output/oak-final-verification/second-run/`; all 67 recorded process identities were absent afterward. The final independent review covers the integrated controls and presentation changes, including the test-only repair and final native evidence. This verifies the bounded checkpoint. Physical falling dynamics, material attachment, resolved root biology, full composition and sustained 60 Hz remain unmet goals.

## Five falling-leaf prototypes did not establish physical contact

The shipped `oak-leaf-fall-path.ts` remains an authored route: it moves a detached body outward to a 155 mm radius, descends to a 15 mm hover, approaches a selected site and settles. Ten landing radii run from 120 to 169.5 mm. Gravity, mass and contact impulses do not determine that route. Existing continuity, support and pool-transfer gates bound its choreography; they do not establish physical free fall. The retained hinge counterexample still places the kinematic datum 0.223767138773 mm outside petiole material, and the public attachment sweep still permits the separately measured 4.791602945 mm separation. Neither gap was repaired in this session.

The first probe named the shipped mechanics, fall-continuity and soil-surface instruments before adding ignored experiments. Five substantive Rapier attempts tested a frozen day-239 leaf for 240 steps at the shared 60 Hz rate. Its 436 source cells and 0.4575 g fresh mass were fixed across comparison arms. The 230 collision covers bridge source clefts of at most one micrometre; they are a bounded approximation, not exact solid unions. The source artifact `output/oak-physics-prototype/source.json` has SHA-256 `73DB04317F29161CC056154D4E565E26B6AE263BE2A6791DF525C38337C28B82`. This evidence predates the new camber and cannot validate its geometry.

Warm-world contacts first allowed 2.641756 mm maximum source penetration. Rebuilding from pose each tick did not produce credible rest. A metre-scale tolerance and predictive-contact pass still reached 0.449658520 mm penetration; an independent collider reconstruction agreed within about 13 nanometres and Rapier reported 0.449646264 mm contact depth, so this failure was not just the source-corner instrument. Exact stepped-terrain exterior triangles with internal-edge correction then reached 1.540482723 mm penetration. The fifth attempt changed contact spring frequency to 1000 Hz while preserving the 60 Hz simulation step. That is a contact-constraint parameter, not a faster solver lane.

The fifth still-air drag arm had a minimum signed source-corner clearance of +8.033 micrometres and ended at +19.358 micrometres. The same frozen source penetrated by 349.490 micrometres with drag removed, 341.529 micrometres under reversed wind and 320.542 micrometres when restoring a serialized world each tick. Positive wind carried the body beyond the finite plot, where no terrain support exists. Final rest alone concealed these transient failures. The five-attempt repair budget ended without accepted dynamics, production physics changes or weakened solidity gates. An eventual physical implementation must account for exported biomass at the domain boundary rather than force a return or deposit; that accounting is not implemented here.

The fifth baseline measured 6.30 ms median and 10.75 ms p95 for one body's `world.step`; reversed wind measured 13.99 and 20.47 ms. These concurrent-workstation observations exclude host, renderer and rebuild costs and cannot establish ten-leaf 60 Hz performance. The energy probe's initial centre-of-mass reading was not independently reconciled, so its apparent first-step energy increase is untrusted and no energy-conservation result is claimed. The selected production state and determinism contract remain unproved.

Retained ignored evidence is under `output/oak-physics-prototype/`, including `probe-fifth.ts`, `probe-fifth.jsonl` and `probe-fifth-energy.jsonl`. The last probe is reproducible from the repository root with the following command; it is retained counterevidence, not a passing gate.

```powershell
node --input-type=module -e "import { createServer } from 'vite'; const server = await createServer({ server: { middlewareMode: true }, appType: 'custom' }); try { await server.ssrLoadModule('/output/oak-physics-prototype/probe-fifth.ts'); } finally { await server.close(); }"
```

The method used the installed Rapier package, its [CCD guidance](https://rapier.rs/docs/user_guides/javascript/rigid_body_ccd/), [collider guidance](https://rapier.rs/docs/user_guides/javascript/colliders/) and versioned [contact-constraint equations](https://github.com/dimforge/rapier/blob/v0.30.1/src/dynamics/integration_parameters.rs), alongside NASA's [falling-object drag model](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/falling-object-with-air-resistance/). Those sources explain the proposed laws, not validation of a leaf model. The existing hinge, mechanics, seasonal-resource and water-pathway unit suites separately passed 18 tests in four files; their resource and kinematic bounds do not resolve the rejected contact dynamics.
