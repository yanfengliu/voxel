# 2026-09-08

## Oak collision queries retain actual affine geometry

The private oak collision predicate now preserves the real stored-matrix column halves, a proved scalar error filter and interval/exact fallback. The strict 2^-39 m threshold, all 15 raw axes and exact-zero-only omission replace dimensionally inconsistent cross cutoffs. Direct OBB calls reach the same predicate. Thin valid shear is accepted deliberately; singular, nonfinite and projective matrix inputs are rejected. This changes collision queries and their diagnostic receipts, not source geometry, biology, solver laws or authored fall placement.

Integration review found four caller conditions that a correct narrowphase alone could not repair. Rounded AABB endpoints could reject identical solids at center 1e20. Full-EPS trimming on both candidate shapes could remove a valid shared grid bucket. Litter paths used AABB hits as final intersections, and the living-litter cache keyed only bounds, allowing an orientation change to retain a stale acceptance. Outward bounds, a proved half-EPS contraction with a thin-interval guard, actual record SAT and a matrix-aware disposable fingerprint address these conditions. Weather has an explicitly named helper retaining its prior trimmed-interior occupancy. Source comparisons at day 20, day 100 and one ordinary breeze frame retained exact matrices/colors, weather output and parent selections.

The first conservative closed-boundary grid increased day-100 keys from 49,685 to 94,067 and candidate pairs from 28,072 to 83,764. Full scans grew from 132.7 to 327.7 ms in that measurement. The actual 120-frame breeze test then exceeded its unchanged 120,000 ms budget, completing at 131,902 ms with native exit 1. Half-EPS contraction restored the original key and pair counts on the same source frame. Scoped source instrumentation separately attributed 655 full metric receipts to 172–190 ms per day-100 frame. A scalar metric extension reuses the proved numerator/norm bounds and returns outward intervals only at the existing EPS/16 precision; ambiguity falls back. The breeze test then passed in 30,292 ms, using 25.2% of the same budget. Host load differed between these runs, so these elapsed values are receipts, not a portable speedup claim. A paired final source sample measured baseline/integrated day-100 build costs of 43.52/70.99 ms still and 53.95/71.43 ms in breeze; it is not FPS or GPU evidence.

The first combined affected run was stopped externally after 349.95 seconds. An initial note wrongly called that a timeout based on a 120-second maximum. The cold-cache continuity case actually has a 514.404-second budget derived from its 117.351-second baseline. The stop therefore proves neither a case timeout nor a synchronous hang. Both the original record and correction remain in ignored evidence, alongside the separate real 120-second breeze failure.

Root review caught two further transfer omissions: the fallback still used implementation-approximated exponentiation for powers of two, and record validation accepted projective bottom rows while treating them as affine. Powers now come from exact binary64 fields, and both record entry points require [0,0,0,1]. Permanent regressions cover these cases. The final algorithmically independent integer-grid metric check has 656 pairs and 9,696 nonzero axes, with zero missed enclosures or decision disagreements. Mutation evidence also records that restoring only the old wrapper stays green with new outward bounds; restoring both old wrapper and old bounds fails. The [gate proof](../../learning/gate-proofs.md#oak-affine-collision-preserves-raw-axes-candidates-and-metric-enclosures) gives the mathematical bounds and test limits.

Independent integrated review rejected that first freeze: three of 96 exact matrix-pair controls lost real geometry while the wrapper divided subnormal columns by two. A separate normal-scale direct-OBB case lost the positive margin in an axis-times-half-length product. The correction preserves the stored matrix cube and stored OBB field products as real operands. Exactly representable record halves retain the scalar path; lossy halves or OBB products enter the existing fallback with original exact dyadics. There is no new scale floor or duplicate solver. The old conversions each fail the newly pinned whole-class regression. The numeric record-to-OBB convenience view remains approximate and never feeds record predicates.

The revised implementation is bound by source-set SHA256 `27082f4c1747e08b5b92ed9a4157ea186431c88e0f47d6388a72fa826bdab8a9` and tracked diff SHA256 `e056767fb9819ea933c758a6c0a5c2e5c244328e440de78bec5f35d2be704af7`. The tracked diff omits new files; `output/oak-physics-continuation/collision-integration/code-freeze-round2.json` binds all 14 complete source/test files. Independent round-two review accepted these bytes with no remaining finding; report SHA256 is `8202d3f88fcb2c029dcdd61f5161f0c36fc44189c46c1c8454db782dbfafba72`. The first freeze and its rejection remain retained. Raw logs and exact-oracle inputs remain ignored. `impact_check` owns implementation; `material_compiler` supplied independent integrated review; root owns acceptance and the final full gate. Root accepted the bounded SAT increment for local delivery on main. The complete oak beauty/physics goal remains active, including flight and material attachment. No push is authorized.

Round-two checks passed 19 focused tests, scoped lint, typecheck, the repeated 656-pair metric oracle and both presented-solidity cases. The 120-frame breeze case took 29,630 ms. The seven-file seasonal/material run on the preceding freeze passed 26 tests in 339.95 seconds; it is retained as preceding-freeze evidence; the final full gate below covers the revised bytes. A final isolated source comparison retained all matrix/color, weather and parent-selection hashes, with day-20 build 42.41/44.10 ms and day-100 build 48.48/80.75 ms still, 54.30/74.11 ms breeze for baseline/integrated arms. One earlier round-two sample began before the preceding typecheck session had been confirmed complete, so it is excluded from the paired-cost claim; the isolated repeat followed confirmed native exit 0. No browser or FPS claim follows from these source construction measurements. All task commands exited, and the final targeted process query found no remaining task-owned Node or esbuild process matching the collision runners.

Final `npm run verify` on the unchanged round-two code passed with native exit 0 in 1,422.218 seconds: 295 unit files and 2,305 tests passed, with the one opt-in windmill physics-search worker skipped. All 135 browser tests passed in 14.5 minutes, with no skips, retries or flaky cases; the slowest budget use was 81.1/180 seconds (45%). Build, typecheck, lint, compatibility, public API, package and supply-chain checks passed. The 939-file source digest `8d3f3290bf0770e912ca70f5a3b9a401c88a285ec12cb061a0efc4c26b1091be`, all 71 baseline bytes and unrelated AGENTS bytes stayed unchanged. All 403 observed owned process identities were gone at cleanup. Raw receipts remain in ignored `output/oak-physics-continuation/collision-integration/final-verification/`.

The full unit phase took 419.53 seconds, compared with 210.17 seconds at the earlier accepted checkpoint. Those are complete-run receipts with different host conditions and workloads, not an isolated algorithm-cost or FPS comparison. The retained paired source measurements above describe the narrower affected cost.

Nine new native 1280 × 720 frames cover hero, side and overhead at day 100 still, day 100 breeze and day 249 late litter. The capture uses real camera/wind/pause controls and 850 ms of ordinary RAF before pausing breeze; milestone advancement uses the existing harness. Capture native exit 0 took 46.528 seconds, with unchanged source/baseline/AGENTS bytes and zero remaining of eight observed owned processes. The author and root inspected all nine individually at original size; root accepted the bounded SAT visual regression check against manifest SHA256 `d59e0582ade0196827f3ef7776f92d7528c849a04da19b7a0dea31891504870b`. Existing attachment gaps, board-like leaf edges, soil seam marks, cropped foreground and arranged litter remain. These frames do not establish physical flight, material attachment or sustained FPS. The earlier `d129905e` controls/beauty checkpoint remains separate evidence.

Only task documents changed after the successful code gate. Historical work-folder allocation and authored-review promotion remain pending under the active-owner preservation exception. All round-one rejection, round-two review and proof bytes remain retained; no historical files or AGENTS bytes were changed.
