# Oak appearance, controls, and physics

Status: the controls/presentation checkpoint was committed as `d129905e2e8e29871b7efd38089707b296a6a58e`. The original whole beauty/physics outcome remains incomplete. The user renewed continuation authorization: “Keep going. You have more limits. Use goal mode to drive yourself and the implementer.” External canon delivery advanced HEAD to `2cf7e445dbba8936d7b3f71cca627df52bb0bef8`; this task preserves those policy bytes and the unrelated outside-block edits. The bounded SAT integration below now changes private production collision queries and their derived diagnostics. Geometry, biology, solver laws, authored fall motion, dependencies, budgets and baselines remain unchanged. No push is authorized.

## Renewed physics benchmark

The coordinator owns the active goal, research assignments, interfaces, attempt accounting, and acceptance. Current owners are `impact_check` for the released bounded SAT production integration and this active plan, `material_compiler` for the fresh independent free-motion investigation after completing the rejected attempt-11 material export controls and independent SAT review, and `contact_implementation` for the generic contact investigation and Gauss/Newton cost optimization in new ignored variants. `collision_delivery` completed the preserved scalar prototype but could not resume its task; root explicitly transferred production ownership. `policy_sync` supplied the preserved benchmark recovery and common input/measurement contract. The primary goal remains the complete oak beauty and physics outcome, with a committed result on main and no push; an accepted numerical-query increment does not satisfy it. Benchmark evidence lives under ignored `output/oak-physics-continuation/`.

The SAT integration preserves strict 2^-39 m depth and all 15 raw axes, uses a proved scalar filter with interval/exact fallback, and retains signed metric enclosures. Outward AABBs and a bounded half-EPS candidate-grid contraction preserve candidate inclusion; actual record SAT makes final litter decisions. Matrix geometry participates in the disposable collision-cache dependency. Weather keeps its separately named trimmed-interior occupancy helper. Affine record semantics are the real stored matrix applied to the unit cube; lossy subnormal halves and direct OBB field products enter the same existing fallback with exact dyadic operands. No valid scale floor was introduced. Record-to-OBB decomposition remains a numeric convenience view and never supplies predicate authority.

The first integrated review found three subnormal matrix-conversion failures among 96 independent pairs and a normal-scale direct-OBB product failure. Those round-one bytes and counterexamples remain frozen. Round two adds exact operand preparation, signed odd-subnormal and lossy-product regressions, and separate assertion-red mutations for the two old conversions. Its 14 production/test files are frozen with source-set digest `27082f4c1747e08b5b92ed9a4157ea186431c88e0f47d6388a72fa826bdab8a9` in `collision-integration/code-freeze-round2.json`. The 19 focused tests, scoped lint, typecheck, repeated 656-pair metric oracle and ordinary source matrix/weather/parent parity pass. Both round-two presented-solidity tests pass, with 120 breeze frames taking 29.630 of 120 seconds. Round-one affected checks passed all 26 tests in seven files in 339.95 seconds; cold-cache continuity used 210.207 of its 514.404 seconds. The earlier 131.902-second breeze timeout and the corrected external-stop record remain retained. Independent round-two review accepted the frozen increment with no remaining finding; its report digest is `8202d3f88fcb2c029dcdd61f5161f0c36fc44189c46c1c8454db782dbfafba72`. Final `npm run verify` on the unchanged round-two code passed with native exit 0 in 1,422.218 seconds: 295 unit files and 2,305 tests passed, with the one opt-in windmill physics-search worker skipped. All 135 browser tests passed in 14.5 minutes, with no skips, retries or flaky cases; the slowest budget use was 81.1/180 seconds (45%). Build, typecheck, lint, compatibility, public API, package and supply-chain checks passed. The 939-file source digest `8d3f3290bf0770e912ca70f5a3b9a401c88a285ec12cb061a0efc4c26b1091be`, all 71 baseline bytes and unrelated AGENTS bytes stayed unchanged. All 403 observed owned process identities were gone at cleanup. Raw receipts remain in ignored `output/oak-physics-continuation/collision-integration/final-verification/`.

Nine new native 1280 × 720 frames cover hero, side and overhead at day 100 still, day 100 breeze and day 249 late litter. The capture uses real camera/wind/pause controls and 850 ms of ordinary RAF before pausing breeze; milestone advancement uses the existing harness. Capture native exit 0 took 46.528 seconds, with unchanged source/baseline/AGENTS bytes and zero remaining of eight observed owned processes. The author and root inspected all nine individually at original size; root accepted the bounded SAT visual regression check against manifest SHA256 `d59e0582ade0196827f3ef7776f92d7528c849a04da19b7a0dea31891504870b`. Existing attachment gaps, board-like leaf edges, soil seam marks, cropped foreground and arranged litter remain. These frames do not establish physical flight, material attachment or sustained FPS. The earlier `d129905e` controls/beauty checkpoint remains separate evidence.

Root accepted the independently reviewed and locally verified SAT increment for local delivery on main. Only task-document updates follow the code freeze, with scoped document checks. The complete oak beauty/physics goal remains active. No push is authorized.

Current material/contact status remains separate. `material_compiler` rejected attempt 11's Float64 thin-annulus export controls; no six-history attempt-11 scoring or production material change was accepted. The contact owner established two contact locations along diagnostic motion, but the actual source free trajectory failed an independent ODE accuracy check at 0.156378 normalized error against a 0.002 limit and gained energy. The old free-motion chart is rejected. Two fresh-owner spawns failed with the actual task limit, so `material_compiler`, which did not author that chart, now owns the separate ignored `free-motion/` investigation. Initially slipping impact work continues, pinned dependencies remain frozen, and CPU-heavy work must serialize with SAT verification. None of these diagnostics is full source-benchmark acceptance.

Earlier recovery checkpoint: attempt 8 resumed under `contact_implementation` with an active goal. Its recovered primitive gate passed 140 cases and an independent preflight passed 63 bounded planar/transformed controls. These were instrument controls, not source-material contact acceptance. The owner was implementing a generic three-dimensional material-vertex/terrain-face increment. `material_repair` checked whether the earlier birth-only source's three petiole boxes represented the physiological leaf mass and area; the reported implied density of approximately 133,000–287,000 kg/m³ was a falsifier to resolve before conservative biomass-derived geometry. No new attachment was accepted. The ledger was instrument investigation; the next actual material construction at that checkpoint was attempt 9.

Before production release, `collision_delivery` completed two distinct bounded ignored passes while preserving the accepted reference, earlier candidates, source input and contract. Allocation removal in `collision-delivery/` retains exact receipts on all 5,415 ordered timing-corpus pairs and bit parity on 60,636 interval products and dot sums, but measured 8.86–13.13 µs per complete call and does not close the speed gap. The subsequent `collision-scalar/` candidate uses a documented conservative forward-error filter and the preserved interval/exact fallback. All 22 fixed controls, metric-enclosure parity, 21 extremes, three invalid-input cases, and 193,814 original plus 193,814 globally rotated source pairs pass. A separately implemented common-grid BigInt SAT agrees on 8,436 pairs: the 5,415 fixed corpus pairs, 3,000 signed random pairs, and 21 extremes; 7,800 scalar decisions and 636 fallback results contain zero incorrect fast decisions. Six paired whole-call timing rounds measured 5.03–7.69 µs for this scalar candidate and 11.21–18.09 µs for robust-fast. Those timings include validation and fallback and establish no browser or solver budget. Scalar code digest is `620cf4030d9c7cd911189576bc8564ca192f946753922037d0b524ff7bf2d46c`; its authored proof digest is `0972ece842395ece7f0177bb39945130ed04da53f7eb7d28ce727e51701d5eea`. Root and an independent numerical review accepted the frozen method with seven transfer conditions before releasing the bounded implementation above.

Canonical work-folder migration remains coordinated separately. Read-only inspection found no existing `docs/work` allocation record or common reservation/lock, but the historical mapping has not been agreed. This active plan and its retained authored reviews remain in place under the work-doc policy's active-owner preservation rule; no ID was allocated ahead of that mapping and no historical document or sibling repository was moved. The final gate and capture cleanup found no remaining task-owned processes; CPU ownership has been released for the continuing independent investigations. Both Vite SSR and capture servers close in `finally`.

The initial renewed wave tracked five additional substantive physics attempts, numbered 6–10 across all delegated mechanism families, within the user's ongoing authorization. This accounting was not a new automatic stop that overrides that authorization; the coordinator owns subsequent bounded assignments and reassessment, including attempt 11 above. Benchmark recovery does not tune a candidate. Reassess after two failures with the same cause; do not repeat an exhausted construction. The accepted benchmark source, matrix, denominators, and criteria stay fixed within each declared experiment.

Attempt 6 belongs to `physics_audit`'s dynamics family and is rejected. Attempt 7 belongs to `workflow_evidence`'s attachment feasibility work and is rejected. At the earlier handoff, attempt 8 was not released: `contact_modes` was preparing a separate contact-method proposal and `policy_sync` owned the ignored certified geometry instrument, with independent review by `robust_contact` and `physics_audit`. Current attempt-8 ownership and evidence are recorded above. The benchmark uses primary leaf 7 and preselected first-detachment donors 27 (minimum occupied volume) and 18 (maximum width/length), selected before candidate results. Uniform density over the paid source solids is an explicitly approved rigid-body reduction, constrained to each leaf's authoritative fresh mass; it does not claim measured tissue density.

Attempt 6 ran all 15 source/forcing cases diagnostically: four held-out wind cases completed by leaving the finite plot, while 11 stopped without consuming the required host time or reached the contact-query cap. Only 1,072 of the required 3,600 steps completed; none established supported rest. All case states and failure receipts remain retained for independent rescoring. Solver-only timings omit parts of oracle and output work, and the energy diagnostic does not independently recompute every receipt operand. Passing analytic controls and truncated restore parity do not establish benchmark or runtime acceptance.

Attempt 7 failed its first mapping census: organ 23's claimed parent 22 belongs to canonical material owner 20, while organ 9's day-24 parent terminal row changes later. The birth-only organ-7 case passes volume, finite contact and foreign-material checks but has only three petiole records and no lamina. The 40-milestone, 120-breeze and browser gates were not run. A separate emergence mismatch remains open: source petiole length is approximately 5.994 mm while the mechanical beam is 0.373 mm. Attempt 8 had not been released at that historical handoff.

Baseline recovery reproduced the fifth probe's independent transient penetration failures. Its apparent first-step energy gain is an instrument error: initial queried COM still equals the body origin until mass properties initialize. Independent geometry places COM higher; the recorded first-step energy is below that corrected initial potential. The new benchmark must reconcile queried and geometric COM before time zero without discarding a physical step. Original source and energy evidence remain preserved.

The recovered fifth matrix exited 0 as an experiment but remains red evidence: no-quadratic-drag, reversed-wind, and serialized-restore corner penetrations were 0.349490, 0.341529, and 0.320542 mm. Positive wind's null final gap is finite-plot outflow, never supported rest. The retained source digest is `73db04317f29161cc056154d4e565e26b6ae263be2a6791df525c38337c28b82`; the new current-source input is separate at `output/oak-physics-continuation/current/input-draft.json`, digest `326f9130ec448a85868c4b3405f4383e661b47d6b8d860d823505bfe7fb195e0`.

The accepted common input fixes 3 sources, 15 source/forcing variants, 3,600 host-step receipts, 1,600 soil columns and 60,882 occupied soil voxels. Calibration checked all 193,814 selected source pairs: minimum material gap is approximately 1 µm, above the fixed 1.819 pm Float64 geometry epsilon. Full-solid SAT catches an edge crossing missed by every source corner. Full-tensor mass quadrature, rotated geometry/tensor covariance and initial soil separation pass. The separate 0.954 µm binary32 presentation allowance never excuses solver penetration. `current/contract.json` fixes continuous forces, contact coefficients, before-step 2–240 restore schedule, partial/full outflow classification and energy receipts; each candidate must disclose its integrator and independently checked numerical residual. Detached donor starting poses remain isolated fixtures, not fracture continuity evidence. The input and contract remain fixed while the separately reviewed geometry instrument is repaired. Full ten-leaf, standing-tissue/prior-litter contact, biomass export and ordinary host performance remain integration gates.

Independent review found a dimensional error in the copied SAT instrument: comparing a half-edge cross product in m² with a metre-valued epsilon can drop a valid near-parallel separator. Normalizing edges did not complete the repair: independently exact arithmetic gives a rotated flush control depth of 2.21 × 10⁻¹⁸ m where the revised floating oracle reports 3.475 × 10⁻¹² m. A separate axis-aligned threshold control shows that accurate cross products alone cannot certify the strict overlap decision. Both rejected oracles and their fixed counterexamples remain preserved.

The ignored reference under `output/oak-physics-continuation/robust-instrument/` uses outward floating intervals and exact dyadic fallback on raw affine half edges, certifies AABB rejection, and skips only exactly zero axes. Boolean overlap and enclosed metric depth are separate. All 18 near-parallel and four threshold controls agree with independent BigInt/Fraction references; face contact, shear, eight square-root endpoint checks, all 193,814 source pairs and their globally rotated copies pass. Independent review accepted classification and the exact-square endpoint repair. Complete predicate calls cost 13.88–39.46 µs per pair on the fixed 5,415-pair corpus, versus 1.20–2.79 µs for the current predicate. This is an isolated correctness reference, not runtime acceptance. Production promotion and any measured fast-path optimization require coordinator review; no production oracle edit has landed.

Fixed reproduction: first verify the retained `output/oak-physics-prototype/source.json` digest against the devlog and rerun `output/oak-physics-prototype/probe-fifth.ts` with its recorded Vite `ssrLoadModule` command into new `output/oak-physics-continuation/baseline/` evidence. Preserve the old source and fifth-attempt outputs. Then extract current paid leaf material from `d129905` and the actual stepped soil into a separate frozen benchmark. Run every variant for all 240 shared 60 Hz steps: still air with drag, no drag, reversed wind, positive wind, and serialized restore. Record source geometry, mass/inertia, transforms and COM with independently checked units; per-step clearance/contact/energy and solver timing; exact nonempty source and step denominators; and source digests. Old pre-camber evidence remains separate from this current-source benchmark.

Done condition: shared 60 Hz force-driven motion using common source-owned material and mass; no authored escape, hover, landing, or renderer-only repositioning; contact depth measured at every step against independently justified numerical tolerance fixed before candidate tests; supported rest where terrain exists; accounted finite-domain outflow; continuous deterministic state with reconciled energy; unchanged biology pools and Space/manual-advance semantics. Continuous collision detection may resolve certified event times inside a host step; all internal work counts in per-tick timing and contact proofs. Ambiguous rate changes go to the coordinator. Solver-only timing makes no browser/runtime performance claim.

Disqualifiers: final-rest-only checks; rounded or corner-only proxies that miss occupied-material penetration; collider inflation or unpaid tissue; moving assertions, geometry, or soil to protect a result; tuned-wind/pose-only testing; smaller denominators; hidden fixed 240/1000 Hz stepping or downsampled claims of 60 Hz; omitted bad variants; replay replacing live simulation; unreconciled COM energy; hidden overlap; or runtime claims derived only from solver timing.

## Completed controls/presentation checkpoint

The root agent coordinates assignments, interfaces, review, and acceptance without implementing. `policy_sync` owns Space controls, the camber trial and attachment search, final verification, and this tracker. `workflow_evidence` owns presentation, material color, HUD, visual evidence, baselines, and the keyboard test observer repair. `physics_audit` owns physical assessment, independent review, and the four companion design/learning/devlog documents. Production source and baseline bytes stayed unchanged during final verification.

The control change reuses the existing Studio keyboard router and oak pause command. It changes no simulation or renderer contract. Baseline capture uses its own server on port 5193; focused keyboard tests use an ephemeral port. Captures must name the source revision and any concurrent control-only changes. Freeze the relevant scene sources during each visual or physical comparison.

Acceptance requires Space to toggle once per press while preserving native controls, paused biological and weather clocks, live camera navigation, and resume without a time jump. The existing oak scene must show the accepted visual improvement in native-size views across relevant angles and conditions, including motion videos. Detached motion and material support must be physically plausible within the fixture's stated limits. Targeted proofs, independent review of the integrated changes, and the final complete `npm run verify` are required before completion. Report unresolved physical or visual limits explicitly.

Checkpoint handoff: Space toggles once per press, preserves editable/native controls, stops biological and weather clocks, leaves camera navigation available, and resumes without catching up paused time. Distributed camber reduces repeated lamina walls; revised lighting, soil color, pinned status controls, and whole-leaf senescence color complete the bounded presentation increment. Independent review accepted the integrated changes and the final test observer repair. No production falling-dynamics, attachment, or camera change landed. Falling-contact repair exhausted its first five attempts; the renewed budget above now governs continuation. Attachment stopped after three rejected families because no justified fourth construction was found. Both physical findings and their counterevidence remain open.

Final gate: exact `npm run verify` passed with exit 0 in 20m54.793s. Unit results were 294 files and 2,288 tests passed, with one explicitly opt-in physics-search worker skipped; browser results were 135 passed in 15.4m with no skips, retries, or flaky cases. The slowest browser budget use was framing at 87.1/180s (48%). Build, typecheck, lint, compatibility, public API, package checks, and supply-chain checks passed. Source digest `1403256fc5cf2da15723b4b0fca4d814b9a054ee787fd3a4c91ff6dbdbb75db4` and all 71 baseline bytes stayed unchanged across the run. Exact logs, start/end manifests, exit metadata, and proof that all 67 recorded process identities were gone remain under ignored `output/oak-final-verification/second-run/`.

The first full command exited 0 with 134 passing browser cases and one flaky pause-clock test, so it was rejected as clean acceptance. Its trace exposed an unacknowledged test listener registration; the test now waits for installation and reads the observed event through a bounded assertion. The original timeout trace and first-run log remain under ignored `output/oak-final-verification/`. The earlier all-mode baseline refresh also retains its 173.1/180s (96%) headroom failure; the ordinary comparison then passed at 99.8/180s (55%). Neither earlier result substitutes for the final run.

Visual evidence is bound to the inspected bytes: all 35 affected 960 × 720 baseline PNGs were reviewed individually at native size, with set digest `423f63eb44b96704a3956b76837eb88c8982f5b9e5c5fa9d4b399a3bb3c4c2aa`. Ordinary growth, weather, and fall clips contributed 29 individually reviewed 1280 × 720 sampled frames across hero, side, and overhead views. Review is limited to those sampled frames and recorded states. Measured live rates ranged from 3.7 to 13.5 FPS across the clips. Late leaf and litter roles now share russet color; remaining board-like edges, cropped plot views, attachment gaps, scripted outward fall, and arranged landings prevent whole-scene physical/visual acceptance. Evidence and native review manifests remain under ignored `output/playwright/oak-beauty-2026-09-05/workflow-evidence/`.

## Attachment and leaf form search

During the search from base `58caca2` plus the control work, `policy_sync` owned attachment and leaf-form prototypes. `physics_audit` owned dynamics, lifecycle, simulation state/types/snapshot, and settled-pose handoff; `workflow_evidence` owned presentation, lighting, material color, and HUD. Shared tissue projection changes required coordination. Prototype evidence stays in ignored output; existing counterexamples remain unchanged until independently accepted replacement proof exists.

Fixed reproduction: `npx vitest run fixtures/oak-ecosystem-consumer/oak-cellular-leaf-hinge.test.ts`. The retained rolled-section case measures the attachment datum 0.223767138773 mm outside actual petiole thickness even though its two support-plane equations pass. All mechanism families must retain and explain this control, then prove finite-area parent/petiole contact using actual occupied source solids at every exposed growth milestone and through 120 breeze ticks. Success also requires no foreign positive-volume overlap or unpaid tissue, preserved target mass and area, and exact pre/post-fracture handoff. New proof must independently inspect material, not reuse the assertion under test.

Disqualifiers: wider blades or collars; decorative bridge cubes; moved assertions; treating a support envelope as occupied material; passing one state only; positive overlap hidden by voxel union; camera, offset, or depth tricks; altered biology solely for appearance; increased tissue mass/area without payment; or attachment that disappears at fracture or under breeze.

The initial mechanism families are a deforming proximal petiole section whose actual end face follows occupied parent material, and a partitioned material junction with a shared parent/leaf interface. These are independent constructions, not position-offset variants. Leaf-form work must identify why current lamina samples produce rails and change the common paid source geometry used by physical and rendered representations. Purpose: make the biological lamina area and attachment load path legible; removal or relocation must lose actual blade tissue or the physical interface, not merely change color. Prototype results precede root acceptance and broad source edits.

First prototype review: neither attachment construction passes. Aligning the first paid petiole box to an occupied parent face conserves its volume but disconnects the next segment and can hit other organs. A single sheared box reaching the existing distal point changes material volume and still cannot join differently oriented end faces. These routes are rejected in their current forms; a lofted material transition would require an agreed convex-solid contract with the physics owner. The retained source-projection counterexamples also include a separate synthetic 2 mm leaf clearance, so correcting the kinematic datum alone cannot restore rendered contact.

The distributed-camber trial keeps the same paid sample counts, blade width, and rise while moving transverse risers away from the two repeated central walls. Across the three complete reference masks, longest three-cell-high columns drop from 28–30 rows to 3–4 rows; narrow sections still contain short three-cell columns. Before integration, native 960 × 720 hero, side, and overhead views at day 180 were inspected, 22 focused anatomy/projection/hinge/material tests passed after restoring a mutation, and five public-solidity/fracture-continuity tests passed. Reintroducing the old risers made the broad-column gate fail at three cells where at most two are allowed. The gate covers three leaf variants, three length/area pairs, and six growth prefixes; it does not establish material attachment or detached dynamics. The final integrated captures and full verification are recorded above.

The third attachment family curves the whole existing petiole from an occupied parent face. In 40 attached-leaf cases at six milestones it obtains a finite shared contact patch, but 22 cases overlap other organs and convex-segment volume differs from the target by −0.1293% to +0.0283%. It is rejected without production attachment changes. The full 120-tick breeze gate was not run because milestone failures already disqualify it. The search stopped after three attachment families with two authorized attempts unused. The earlier analytic gap, 5.45996 mm maximum raw-source gap, and public-port counterexamples remain intact.

Reassessment has not justified a fourth attachment trial. Opposite end faces of an affine source box remain parallel under shear, so joining a parent-normal face to a differently oriented leaf section requires a different material construction. A polyhedral junction would need an agreed occupied-volume and contact recipe across growth and hinge motion before source implementation. This is a limitation of the tested constructions, not a proof that attachment is impossible. Camera refit and full-plot framing are separate read-only proposals; existing hard framing and free-navigation gates remain requirements.

The camera study compares paired fits of the same accepted day-20, day-180, and day-249 geometry at 1280 × 720 and 960 × 520. Including conservative bounds of the actual occupied soil keeps the full plot visible, but at day 20 reduces the tree to 31 × 69 px and 22 × 50 px respectively, below the existing minimum-width gate. At day 180 the tree remains 151 × 247 px and 109 × 178 px, about half its former height. A universal full-plot Hero therefore trades away early growth inspection. Automatic refits currently jump after crossing a retention margin; unqualified easing would violate the existing hard displayed-fit bounds. No camera source change is proposed for this increment without an accepted solution to those conflicts; all litter remains part of the focus census.

The Overhead-specific follow-up also fails the existing early-stage inspection contract: full-plot framing reduces the day-20 plant to 32 × 32 px and 23 × 23 px respectively, with horizontal NDC spans of 0.050 and 0.048 against the required 0.15. The coordinator therefore declined this camera increment. No camera production files or framing assertions changed, and the final visual capture freeze was released.

Biological references support treating the petiole/blade transition as graded tissue and abscission as separation within tissue, not an air gap: [Langer et al., 2021](https://pmc.ncbi.nlm.nih.gov/articles/PMC8071152/) and [a 2014 cotton abscission study](https://journals.plos.org/plosone/article?id=10.1371/journal.pone.0097652). Those studies concern other species and do not calibrate this oak fixture's dimensions.
